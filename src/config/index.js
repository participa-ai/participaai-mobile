const asyncStorageKeyBase = '@participaai';

export default {
    apiBaseUrl: 'https://participa-api.herokuapp.com/api',
    asyncStorageKeys: {
        token: `${asyncStorageKeyBase}:token`,
    },
};
